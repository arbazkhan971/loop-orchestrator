# Todo App Brief

Build a small, dependency-free todo app in Node.js:
- A plain HTTP server (node:http) exposing a JSON REST API for todos
- In-memory store with: list, add, toggle complete, delete
- A single static index.html with vanilla JS that talks to the API
- node:test tests for the store and the API handlers

Keep it zero-dependency, small, and well-tested.
